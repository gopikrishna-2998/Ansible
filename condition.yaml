- name: condition
  hosts: local
  connection: local
  vars:
    number: 5
  tasks:
  - name: check number is less than 10
    ansible.builtin.debug:
      msg: "number is less than {{ number }}"
    when: number < 10