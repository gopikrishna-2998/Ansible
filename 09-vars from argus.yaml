- name: vars from arugs
  hosts: local
  connection: local
  tasks:
  - name: course details  
    ansible.builtin.debug:
      msg: "your name is {{ name }}, duration is {{ duration }}, trainer is {{ trainer }}"